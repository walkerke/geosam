% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imagery.R
\name{get_imagery}
\alias{get_imagery}
\title{Download Satellite Imagery}
\usage{
get_imagery(
  bbox,
  output = NULL,
  source = c("mapbox", "esri", "maptiler", "google"),
  zoom = 17,
  api_key = NULL
)
}
\arguments{
\item{bbox}{Bounding box for the area. Can be a numeric vector
\code{c(xmin, ymin, xmax, ymax)} in WGS84, or an sf/sfc object.}

\item{output}{Path for output GeoTIFF. If NULL, creates a temp file.}

\item{source}{Imagery source: "mapbox", "esri", "maptiler", or "google".
\itemize{
\item "mapbox": Requires \code{MAPBOX_PUBLIC_TOKEN} environment variable
\item "esri": Free Esri World Imagery (no API key required)
\item "maptiler": Requires \code{MAPTILER_API_KEY} environment variable
\item "google": Google satellite tiles (no API key required)
}}

\item{zoom}{Tile zoom level (15-19). Higher values = more detail.
Recommended: 17-18 for objects like buildings, swimming pools.}

\item{api_key}{API key for the imagery source. For Mapbox, uses
MAPBOX_PUBLIC_TOKEN environment variable by default. For MapTiler, uses
MAPTILER_API_KEY environment variable by default.}
}
\value{
Path to the downloaded GeoTIFF file.
}
\description{
Downloads satellite imagery tiles for a bounding box and saves as a GeoTIFF.
Uses R-native tools (httr2, terra) for reliable tile fetching and georeferencing.
}
\examples{
\dontrun{
# Download imagery for an area
img <- get_imagery(
  bbox = c(-102.5, 31.8, -102.4, 31.9),
  source = "mapbox",
  zoom = 17
)

# Use with detection
pads <- sam_detect(image = img, text = "swimming pool")
}
}
