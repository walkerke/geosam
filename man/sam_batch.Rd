% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{sam_batch}
\alias{sam_batch}
\title{Batch Process Multiple Areas}
\usage{
sam_batch(
  areas,
  text,
  source = "mapbox",
  zoom = 17,
  threshold = 0.5,
  min_area = NULL,
  max_area = NULL,
  .progress = TRUE
)
}
\arguments{
\item{areas}{An sf object with polygon geometries defining areas to process.}

\item{text}{Text prompt for detection.}

\item{source}{Imagery source: "mapbox", "esri", or "google".}

\item{zoom}{Tile zoom level for imagery download.}

\item{threshold}{Detection confidence threshold.}

\item{min_area}{Minimum object area in square meters.}

\item{max_area}{Maximum object area in square meters.}

\item{.progress}{Logical. If TRUE, shows a progress bar.}
}
\value{
An sf data frame with detection results. Includes an \code{area_id} column
linking detections to input areas.
}
\description{
Runs SAM3 detection on multiple areas (polygons) and returns combined results.
}
\examples{
\dontrun{
library(tigris)

# Process multiple counties
counties <- counties("TX") |>
  filter(NAME \%in\% c("Midland", "Ector"))

pads <- sam_batch(
  areas = counties,
  text = "swimming pool",
  min_area = 500
)
}
}
