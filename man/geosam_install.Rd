% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{geosam_install}
\alias{geosam_install}
\title{Install Python Dependencies for geosam}
\usage{
geosam_install(
  envname = "geosam",
  method = c("uv", "virtualenv", "conda"),
  gpu = NULL,
  hf_token = NULL,
  python_version = "3.12"
)
}
\arguments{
\item{envname}{Name of the virtual environment to create (default: "geosam")}

\item{method}{Installation method: "uv" (recommended, fast), "virtualenv",
or "conda". uv is the default and recommended approach.}

\item{gpu}{Logical. If TRUE, installs GPU-enabled PyTorch. If NULL (default),
auto-detects based on available hardware.}

\item{hf_token}{Optional HuggingFace token for accessing SAM3 model weights.
Can also be set via the HF_TOKEN environment variable. Required for SAM3.}

\item{python_version}{Python version to use (default: "3.12"). SAM3 requires 3.12+.}
}
\value{
Invisibly returns TRUE on success.
}
\description{
Creates a Python virtual environment and installs required packages for SAM3
inference. This only needs to be run once.
}
\details{
This function installs:
\itemize{
\item PyTorch 2.7+ (with MPS support on Apple Silicon, CUDA 12.6+ on NVIDIA GPUs)
\item SAM3 from Meta (https://github.com/facebookresearch/sam3)
\item rasterio, geopandas, shapely, pyproj (geospatial processing)
}
\subsection{Installation Methods}{

\strong{uv (recommended)}: Fast, modern Python package manager. Install uv first:

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl -LsSf https://astral.sh/uv/install.sh | sh
}\if{html}{\out{</div>}}

\strong{virtualenv}: Uses Python's built-in venv via reticulate. Requires Python 3.12+
to be installed on your system.

\strong{conda}: Uses conda/miniconda. Slower but handles complex dependencies well.
}

\subsection{SAM3 Access}{

You must have HuggingFace access approval for SAM3 model weights.
Request access at: https://huggingface.co/facebook/sam3
}
}
\examples{
\dontrun{
# Standard installation with uv (recommended)
geosam_install()

# With HuggingFace token
geosam_install(hf_token = "hf_xxxxx")

# Use virtualenv instead of uv
geosam_install(method = "virtualenv")

# Use conda
geosam_install(method = "conda")
}
}
