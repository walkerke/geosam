% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{plot.geosam_image}
\alias{plot.geosam_image}
\title{Plot geosam_image Detection Results}
\usage{
\method{plot}{geosam_image}(
  x,
  fill = "#FACC15",
  border = NULL,
  palette = NULL,
  opacity = 0.4,
  border_width = 2,
  add = FALSE,
  base_img = NULL,
  ...
)
}
\arguments{
\item{x}{A geosam_image object from \code{sam_image()} or \code{sam_explore_image()}.}

\item{fill}{Fill color for detection masks. Default is yellow.
Ignored if \code{palette} is specified.}

\item{border}{Border color for detection outlines. If NULL (default), automatically
darkens the fill color. Set to NA to disable borders.}

\item{palette}{Optional color palette for distinct colors per detection. Can be:
\itemize{
\item A character vector of colors (recycled if needed)
\item A palette name from RColorBrewer (e.g., "Set1", "Dark2", "Paired")
\item A palette function that takes n and returns colors
}}

\item{opacity}{Opacity of the mask overlay (0-1). Default is 0.4.}

\item{border_width}{Width of border lines in pixels. Default is 2.}

\item{add}{If TRUE, add to an existing magick image instead of loading from file.
Pass the image as the \code{x} parameter's \code{image_path} will be ignored and \code{base_img}
must be provided.}

\item{base_img}{A magick image to add overlays to. Required when \code{add = TRUE}.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Invisibly returns the composite magick image, which can be saved
with \code{magick::image_write()} or further manipulated.
}
\description{
Displays an image with detection masks overlaid using magick.
}
\examples{
\dontrun{
result <- sam_image("photo.jpg", text = "dog")
plot(result)

# Distinct colors per detection
plot(result, palette = "Set2")

# Custom color (border auto-matches)
plot(result, fill = "red")

# Layer multiple detections
img <- plot(result1)
img <- plot(result2, add = TRUE, base_img = img, fill = "blue")
img <- plot(result3, add = TRUE, base_img = img, fill = "red")

# Save the result
magick::image_write(img, "annotated.png")
}
}
