% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{sam_refine}
\alias{sam_refine}
\title{Refine Detection with Additional Point Prompts}
\usage{
sam_refine(x, points, labels = NULL)
}
\arguments{
\item{x}{A geosam object.}

\item{points}{sf object with point geometries, or a matrix/data frame with columns x, y.}

\item{labels}{Integer vector of labels (1 = foreground/positive, 0 = background/negative).
If NULL, all points are treated as positive.}
}
\value{
A new geosam object with refined detections.
}
\description{
Re-runs detection with additional positive or negative point prompts to refine results.
}
\examples{
\dontrun{
result <- sam_detect(image = "satellite.tif", text = "building")
# Add positive and negative points to refine
refined <- sam_refine(result,
  points = data.frame(x = c(-102.5, -102.51), y = c(31.8, 31.81)),
  labels = c(1, 0)  # first is positive, second is negative
)
}
}
