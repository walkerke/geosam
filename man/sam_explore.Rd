% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{sam_explore}
\alias{sam_explore}
\title{Interactive Discovery and Detection}
\usage{
sam_explore(
  source = c("mapbox", "esri", "maptiler"),
  center = NULL,
  bbox = NULL,
  zoom = 15
)
}
\arguments{
\item{source}{Imagery source: "mapbox", "esri", or "maptiler".
\itemize{
\item "mapbox": Requires \code{MAPBOX_PUBLIC_TOKEN} environment variable
\item "esri": Free Esri World Imagery (no API key required)
\item "maptiler": Requires \code{MAPTILER_API_KEY} environment variable
}}

\item{center}{Initial map center as c(lng, lat). If NULL, defaults to US center.}

\item{bbox}{Initial bounding box as c(xmin, ymin, xmax, ymax) or sf object.
If provided, map will zoom to this extent.}

\item{zoom}{Initial zoom level (default 15).}
}
\value{
A geosam object when the user clicks "Done", or NULL if cancelled.
}
\description{
Opens an interactive map to explore satellite imagery, draw prompts, and
run SAM detection.
}
\details{
The explorer provides a complete workflow:
\enumerate{
\item Navigate the map to find an area of interest
\item Select a prompt type (text, box, points, or exemplar)
\item Enter text or draw prompts on the map
\item Click "Detect" to run SAM on the current viewport
\item View results and optionally refine with +/- points
\item Click "Done" to return the geosam object
}
}
\examples{
\dontrun{
# Start exploring with Mapbox satellite
result <- sam_explore(source = "mapbox", center = c(-102.5, 31.8), zoom = 15)

# Extract results
if (!is.null(result)) {
  polygons <- sam_as_sf(result)
}
}
}
