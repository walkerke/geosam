% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{sam_as_raster}
\alias{sam_as_raster}
\title{Extract Raster Masks from geosam Object}
\usage{
sam_as_raster(x, combined = FALSE)
}
\arguments{
\item{x}{A geosam object from \code{sam_detect()} or \code{sam_explore()}.}

\item{combined}{If TRUE, combine all masks into a single layer (union).
If FALSE (default), return a multi-layer raster with one layer per detection.}
}
\value{
A terra SpatRaster with mask values (1 = detected, 0 = background).
Returns NULL if no masks.
}
\description{
Converts detection masks from a geosam object to a terra SpatRaster.
}
\examples{
\dontrun{
result <- sam_detect(image = "satellite.tif", text = "building")
mask_raster <- sam_as_raster(result, combined = TRUE)
terra::plot(mask_raster)
}
}
