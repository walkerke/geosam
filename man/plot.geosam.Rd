% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{plot.geosam}
\alias{plot.geosam}
\title{Plot geosam Detection Results}
\usage{
\method{plot}{geosam}(
  x,
  fill = "#FACC15",
  border = NULL,
  palette = NULL,
  opacity = 0.5,
  lwd = 1,
  main = NULL,
  add = FALSE,
  ...
)
}
\arguments{
\item{x}{A geosam object from \code{sam_detect()} or \code{sam_explore()}.}

\item{fill}{Fill color for detection polygons. Default is semi-transparent yellow.
Ignored if \code{palette} is specified.}

\item{border}{Border color for polygon outlines. If NULL (default), automatically
darkens the fill color. Set to NA to disable borders.}

\item{palette}{Optional color palette for distinct colors per detection. Can be:
\itemize{
\item A character vector of colors (recycled if needed)
\item A palette name from RColorBrewer (e.g., "Set1", "Dark2", "Paired")
\item A palette function that takes n and returns colors
}}

\item{opacity}{Opacity for fill colors (0-1). Default is 0.5. Applied to all fills.}

\item{lwd}{Line width for polygon borders. Default is 1.}

\item{main}{Plot title. Default is NULL (no title).}

\item{add}{If TRUE, add to existing plot instead of creating new one.
Default is FALSE.}

\item{...}{Additional arguments passed to \code{terra::plotRGB()} when \code{add = FALSE}.}
}
\value{
Invisibly returns the sf polygons that were plotted.
}
\description{
Displays satellite imagery with detection polygons overlaid using terra.
}
\examples{
\dontrun{
result <- sam_detect(
  bbox = c(-102.63, 31.81, -102.62, 31.83),
  text = "well pad"
)
plot(result)

# Distinct colors per detection
plot(result, palette = "Set1")

# Custom colors
plot(result, fill = "red")  # border auto-darkens

# Layer multiple results
plot(result1)
plot(result2, add = TRUE, fill = "blue")
}
}
