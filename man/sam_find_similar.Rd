% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{sam_find_similar}
\alias{sam_find_similar}
\title{Find Similar Objects Using Selected Detection as Exemplar}
\usage{
sam_find_similar(x)
}
\arguments{
\item{x}{A geosam object with exactly one detection (use \code{sam_select()} first).}
}
\value{
A new geosam object with all detected similar objects.
}
\description{
Uses a single selected detection as an exemplar to find all similar objects
in the image.
}
\examples{
\dontrun{
result <- sam_detect(image = "satellite.tif", text = "swimming pool")
# View results, pick the best one, find all similar
similar <- result |>
  sam_select(3) |>
  sam_find_similar()
}
}
